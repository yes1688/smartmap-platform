# 開發環境專用 Dockerfile
FROM node:18-alpine

# 設置工作目錄
WORKDIR /app

# 複製 package 文件
COPY package*.json ./

# 安裝所有依賴（包括開發依賴）
RUN npm ci

# 複製源代碼
COPY . .

# 設置環境變數
ENV NODE_ENV=development
ENV PORT=3000
ENV HOST=0.0.0.0

# 暴露端口 3000
EXPOSE 3000

# 啟動 Vite 開發服務器
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3000"]